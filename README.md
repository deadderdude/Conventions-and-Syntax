# Conventions-and-Syntax

myHealth = 0

public int Health(int amount)
    myHealth += amount;
    return amount;
}
    int[] myHealth = damage[2]

    myHealth[0] -= 2;
    myHealth[1] -= 3;

    foreach(int item in myHealth);
    print int
